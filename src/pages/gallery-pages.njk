---
templateEngineOverride: njk
pagination:
  data: gallery
  size: 9
  alias: items
  reverse: true
layout: "base.njk"
permalink: "gallery/page-{{ pagination.pageNumber + 1 }}/index.html"
eleventyComputed:
  title: "Gallery"
  description: "Explore the gallery!"
---
<h1>{{ title }}</h1>
<div class="gallery">
  {% for item in items | sort(attribute='date') | reverse %}
    <figure>
      <a href="/gallery/{{ item.slug }}">
        <div class="gallery__overlay">
          <img src="/gallery/{{ item.image }}" alt="{{ item.alt }}">
          <span class="gallery__text">[{{ item.thumbnailcaption }}]</span>
        </div>
      </a>
    </figure>
  {% endfor %}
</div>
{% include "partials/postnav.njk" %}